#include<iostream>
#include<stdio.h>
#include<vector>
#include<string.h>
int main()
{
    int T;
    scanf("%d",&T);
    int corridors[210];
    vector<int> adj[210];

    while(T--)
    {
        scanf("%d",&chambers);
        for(i=0;i<chambers;i++)
        {
            scanf("%d",corridors[i]);
            for(j=0;j<corridors[i];i++)
            {
                scanf("%d",&temp);
                adj
        }
    }
}
